[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "health_analytics"
version = "0.1.0"
# Notice: dependencies list is completely omitted

[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]